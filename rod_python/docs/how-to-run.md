# How to Run PiCam on Raspberry Pi (python version)

## Running the Main Application
``` shell
source .venv/bin/activate
cd raspberry
python3 -m vision_python.src.main
```

This application start in a virtual environment that is activated with:
``` shell
source .venv/bin/activate
```

You can exit the virtual environment with:
``` shell
deactivate
```